import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  notificationItem: {
    borderBottomWidth: 1,
    borderColor: '#ccc',
    paddingVertical: 8,
  },
  notificationText: {
    fontSize: 14,
    color: '#333',
  },
});
